spring:
  application:
    name: NoCopyZone

  datasource:
    # localhost:5433 o'rniga servis nomi va ichki port 5432
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://postgres:5432/nocopyzone?sslmode=disable}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  data:
    redis:
      # localhost:6380 o'rniga servis nomi va standart port 6379
      url: ${SPRING_DATA_REDIS_URL:redis://redis:6379}
      timeout: 2000ms

  rabbitmq:
    # localhost o'rniga rabbitmq servis nomi
    addresses: ${SPRING_RABBITMQ_ADDRESSES:amqp://guest:guest@rabbitmq:5672/}

  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: 587
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  servlet:
    multipart:
      enabled: true
      max-file-size: 50MB
      max-request-size: 55MB

server:
  port: ${PORT:8080}
  servlet:
    context-path: /api

app:
  jwt:
    secret: ${APP_JWT_SECRET:nocopyzone-super-secret-key-minimum-256-bits-long-for-hs256}
    expiration: 86400000
    refresh-expiration: 604800000

  upload:
    dir: ${UPLOAD_DIR:uploads}

  proctoring:
    log-flush-interval: 5000
    max-tab-switches: 3

logging:
  level:
    com.rayimbek.nocopyzone: DEBUG
    org.springframework.security: INFO